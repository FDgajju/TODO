/*
import axios from "axios";

const fetch = async () => {
  const data = await axios.post("/api/task", {
    title: "nacho",
    Description: "utho",
    Status: "Open",
  });
};
fetch()
*/

function App() {
  return (<h1>Hello world</h1>);
}

export default App;
